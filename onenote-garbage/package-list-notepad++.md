# Package list Notepad++

```
Mark in find window

^.*\..*\..*$    #to remove code levels with 2 periods
^.*\..*$         # to remove code levels with 1 period
KB$               # to remove lines with KB
MB$             # to remove lines with MB
GB$             # to remove lines with GB
[^\d\r\n]    # remove rest of text

Search, bookmarks, remove all bookmarks



Add to acme to get dependencies and download CSV.
```

